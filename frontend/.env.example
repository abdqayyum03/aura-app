# Point this at your backend. On a physical device/Expo Go, "localhost" means
# the phone itself, not your computer - use your computer's LAN IP instead
# (e.g. http://192.168.1.42:3000). On an Android emulator, use 10.0.2.2.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
